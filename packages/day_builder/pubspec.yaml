name: day_builder
description: Dynamically builds Day class based on existing implementations.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  analyzer: any
  aoc_core: any
  build: ^2.0.0
  build_config: ^1.0.0
  dart_style: any
  glob: any
  pub_semver: any

dev_dependencies:
  lints: ^6.0.0
